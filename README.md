# semcap

# Description
semcap is a wrapper library for web developers who want to encapsulate their web applications in a class structure, to ensure strict adherance to schema.org's semantic metadata.

semcap stands for "Semantic Encapsulation", is written in PHP and is currently at version 0.1.0, a pre-alpha stage.
